import { View } from "react-native";

/** La versión web mantiene la pantalla completa de Tormentar; este contenedor
 * solo se utiliza en el APK Android. */
export function TormentarWebContainer() {
  return <View />;
}
